import { PropertiesSalesPageLayout } from '@/features/properties/components/sales/PropertiesSalesPageLayout';

export default function Layout({ children }: { children: React.ReactNode }) {
  return <PropertiesSalesPageLayout>{children}</PropertiesSalesPageLayout>;
}

import { describe, it, expect } from '@jest/globals';

describe('App Layout', () => {
  it('renders root layout with providers (placeholder)', () => {
    // TODO: Test que el layout raíz se renderiza correctamente
    // - SessionProvider configurado
    // - Metadata correcta
    // - Fuentes cargadas
    expect(true).toBe(true);
  });
});

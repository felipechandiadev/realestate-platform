import { describe, it, expect } from '@jest/globals';

describe('BackOffice Layout', () => {
  it('renders backOffice layout with TopBar and Menu (placeholder)', () => {
    // TODO: Test que el layout del backOffice se renderiza correctamente
    // - TopBar con usuario y logout
    // - Menú lateral/hamburger
    // - Breadcrumbs
    // - Footer
    expect(true).toBe(true);
  });

  it('restricts access to authenticated users only (placeholder)', () => {
    // TODO: Test que restringe acceso
    // - Solo usuarios autenticados
    // - Roles apropiados (ADMIN, SUPERADMIN, AGENT)
    expect(true).toBe(true);
  });
});

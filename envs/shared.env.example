# Shared (reference). Copy values into each app via sync-dev-envs.sh

# Core API
CORE_URL=http://localhost:8000
PORTAL_URL=http://localhost:8001
BACKOFFICE_URL=http://localhost:8002

# Auth
NEXTAUTH_SECRET=change-me-in-dev

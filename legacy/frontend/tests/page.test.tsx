import { describe, it, expect } from '@jest/globals';

describe('Root Home - / (redirect)', () => {
  it('redirects to /portal after delay (placeholder)', () => {
    // TODO: Test que la página raíz redirige a /portal
    // - Muestra DotProgress durante 3 segundos
    // - Redirige a /portal
    expect(true).toBe(true);
  });
});
